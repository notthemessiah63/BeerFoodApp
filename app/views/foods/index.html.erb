<h3>Food - Index Page</h3>
<div>
  <% @foods.each do |food| %>
    <div class="list-group-item">
      <ul class="food">
        <li><%= link_to(food.name, food_path(food.id))%></li>
      </ul>
    </div>
  <% end %>
</div>