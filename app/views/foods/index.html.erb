<h3>Food - Index Page</h3>

<container class='siz40'>
  <div>
    <% @foods.each do |food| %>
      <div class="list-group-item">
        <ul class="food">
          <li><%= link_to(food.name, food_path(food.id))%></li>
        </ul>
      </div>
    <% end %>
  </div>
</container>
<container class='siz40'>
  <div>Some Stuff about pairing beer and food</div>
</container>