<h2>Beers - Index page</h2>
<container class='siz30l'>
  <div>
    <% @beers.each do |beer| %>
      <div class="list-group-item">
        <ul class="food">
          <li><%= link_to(beer.name, beer_path(beer.id))%></li>
        </ul>
      </div>
    <% end %>
  </div>
</container>
<li><%= link_to 'Add a beer', new_beer_path %></l1>
<container class='siz60r'>
  <div>Some groovy Stuff about beer</div>
</container>